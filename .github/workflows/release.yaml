name: Release

# Run workflow on commits to the `master` branch
on:
  push:
    branches:
      - main
      - fix-docker-image-pipeline

jobs:
  release:
    runs-on: ubuntu-18.04
    steps:
      - name: Check out Git repository
        uses: actions/checkout@v3

      - name: Install Java and Maven
        uses: actions/setup-java@v3.3.0
        with:
          distribution: 'zulu'
          java-version: '11'

      - name: Release Maven package
        uses: samuelmeuli/action-maven-publish@v1
        with:
          gpg_private_key: ${{ secrets.gpg_private_key }}
          gpg_passphrase: ${{ secrets.gpg_passphrase }}
          nexus_username: ${{ secrets.nexus_username }}
          nexus_password: ${{ secrets.nexus_password }}

      - name: Set up QEMU
        uses: docker/setup-qemu-action@v1
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1
      - name: Login to DockerHub
        uses: docker/login-action@v1
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_KEY }}

      - name: Maven version
        id: get-version
        uses: JActions/maven-version@v1.0.0

      - name: Build and push
        uses: docker/build-push-action@v2
        with:
          context: ./digiwf-email-integration-service
          push: true
          tags: itatm/digiwf-email-integration-service:latest,itatm/digiwf-email-integration-service:${{ steps.get-version.outputs.version }}
